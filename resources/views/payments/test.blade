<table id="datatable-fixed-header" class="table table-striped table-bordered dt-responsive nowrap" cellspacing="0" width="100%">
                      <thead>
                        <tr>
                          <th>&nbsp;</th>
                          <th>First Name</th>
                          <th>Surname</th>
                          <th>Medical Aid number</th>
                          <th>Plan</th>
                          <th>ID Number</th>                             
                          <th>Action</th>                                                
                        </tr>
                      </thead>
                      <tbody>
                        @foreach ($clients as $client)
                          <tr data-toggle="collapse" data-target="#{{$client->id}}" class="accordion-toggle">
                            <td><button class="btn btn-default btn-xs"><span class="glyphicon glyphicon-eye-open"></span></button></td>
                            <td><a href="/clients/{{$client->id}}"> {{ $client->name}}</a></td>
                            <td>{{ $client->surname}}</td>
                            <td>{{ $client->medical_aid_number}}</td>
                            <td>{{ $client->plan->name}}</td>
                            <td>{{ $client->id_number}}</td>                                           
                            <td>
                              <a onclick="viewInfo({{$client->id}})" class="btn btn-primary btn-xs view_{{$client->id}}" 
                              data-name="{{$client->name}}" data-name="{{$client->account_number}}"><i class="fa fa-folder"></i> View Client </a>
                              <a onclick="viewStatement({{$client->id}})" class="btn btn-info btn-xs"><i class="fa fa-pencil"></i> More  Details... </a>
                              <a onclick="deleteData({{$client->id}})" class="btn btn-danger btn-xs"><i class="fa fa-trash-o"></i> Make Payment</a>
                              <form>                               
                                @csrf                                
                              </form>
                            </td>                              
                          </tr>
                          <tr>
                            <td colspan="12" class="hiddenRow">
                              <div class="accordian-body collapse demo1" id="{{$client->id}}"> 
                                <table class="table table-striped">                                 
                                    <tr>
                                      <td>Name</td>
                                      <td>Surnam</td>
                                      <td>ID Number</td>
                                      <td>Name Again</td>
                                      <td>some date</td>
                                      <td>My Name</td>
                                      <td>My Name</td>
                                    </tr>   
                                  </tbody>
                                </table>
                              </div>
                            </td>
                          </tr>
                        @endforeach                        
                      </tbody>
                    </table>